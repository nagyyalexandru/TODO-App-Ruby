<%= form_with(model: [task.todo_list, task]) do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title, required: true %>

  <%= form.label :notes %>
  <%= form.text_area :notes %>

  <%= form.submit %>
<% end %>