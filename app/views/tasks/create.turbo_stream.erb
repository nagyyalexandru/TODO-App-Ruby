<%= turbo_stream.append "tasks_#{@task.todo_list.id}" do %>
  <%= render @task %>
<% end %>

<%= turbo_stream.replace "task_form_#{@task.todo_list.id}" do %>
  <%= render "tasks/form", task: Task.new(todo_list: @task.todo_list) %>
<% end %>